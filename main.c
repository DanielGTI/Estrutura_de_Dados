#include <stdio.h>

// Revisão 01
int main(void) {

	// int, float, double, char, char[10]
	int a, b;
	float x, y;
	double valor;
	char letra;
	char nome[50];
	/*
		+ - * / %
		> >= < <= == !=
		&& || ^
		x++, x--, ++x, --x
	 
		if()
		if() else()
		if() else( if() )
		switch()

		while(?){}
		do{} while(?)
		for(???){}

		int valor[10] (0-9)
		int x[L][C]

		char nome[100] // #include <string.h>

		int nome_funcao(int x){
			return ??;
		}

		void nome_procedimento(int x, int y){

		}

		Variavel Local e Global

		Ponteiros (variavel que armazena endereço de memoria)

		struct
	*/
	return 0;
}